import { memo } from "react";

export const GrammarCheckDescription = memo(() => (
  <section className="mt-4 mb-4 font-jost text-fuchsia-700 text-center md:text-xl">
    <h1>
      Выберите модуль и сторону карточек, которые будут основой для генерации
      предложений теста.
    </h1>
  </section>
));
