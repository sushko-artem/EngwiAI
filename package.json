{
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "test:e2e": "docker compose --env-file .env.test -f compose.test.yaml up --exit-code-from cypress",
    "test:e2e:build": "docker compose --env-file .env.test -f compose.test.yaml up --build --exit-code-from cypress",
    "dev:test": "docker compose --env-file .env.test -f compose.test.yaml up -d frontend_test backend_test nginx_test postgres_test",
    "test:e2e:open": "cd frontend && npx cypress open --config baseUrl=http://localhost:8080",
    "dev:test:down": "docker compose --env-file .env.test -f compose.test.yaml down"
  }
}
